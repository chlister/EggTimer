<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dk.marc.eggtimer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/EggTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".EggSVGActivity">

        </activity>
        <activity android:name=".EggTivity">

        </activity>
        <activity android:name=".EggActivity"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".EggViewActivity" >

        </activity>
        <activity android:name=".EggViewMVP" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>